import React, { useState } from "react";

function RightMenu() {
  // Declare a new state variable, which we'll call "count"
  const [count, setCount] = useState(0);

  return <div className="top-right-menu">Right</div>;
}

export default RightMenu;
