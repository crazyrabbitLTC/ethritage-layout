import React, { useState } from "react";

function RightMenu() {
  // Declare a new state variable, which we'll call "count"
  const [count, setCount] = useState(0);

  return <div class="top-right-menu">Right Side</div>;
}

export default RightMenu;
